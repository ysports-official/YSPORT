name: EAS Android Build

on:
  workflow_dispatch:
    inputs:
      profile:
        description: 'Build Profile (preview: APK debug, production: AAB)'
        required: true
        default: 'preview'
        type: choice
        options:
          - preview
          - production

permissions:
  contents: write

jobs:
  build:
    name: Build Android (${{ github.event.inputs.profile || 'preview' }})
    runs-on: self-hosted

    steps:
      - name: 🔧 Git Bash + Java + Android PATH (Windows runner)
        shell: powershell
        run: |
          Add-Content $env:GITHUB_PATH "C:\Program Files\Git\bin"
          Add-Content $env:GITHUB_PATH "C:\Program Files\Git\usr\bin"
          $javaHome    = "$env:LOCALAPPDATA\Java\jdk-17.0.11+9"
          $androidHome = "$env:LOCALAPPDATA\Android\Sdk"
          Add-Content $env:GITHUB_ENV  "JAVA_HOME=$javaHome"
          Add-Content $env:GITHUB_ENV  "ANDROID_HOME=$androidHome"
          Add-Content $env:GITHUB_PATH "$javaHome\bin"
          Add-Content $env:GITHUB_PATH "$androidHome\platform-tools"
          Add-Content $env:GITHUB_PATH "$androidHome\cmdline-tools\latest\bin"
          Write-Host "JAVA_HOME = $javaHome"
          Write-Host "ANDROID_HOME = $androidHome"

      - name: 📂 Clone to C:\y — Windows MAX_PATH fix (kısa yol)
        shell: powershell
        run: |
          # actions/checkout KULLANMIYORUZ — C:\actions-runner-ysports\_work\... yolu
          # CMake .o dosyalarına bu uzun yolu gömer ve 260 char sınırını aşar.
          # C:\y altına klonlayarak base path 59 char -> 4 char'a düşüyor.
          if (Test-Path C:\y) { Remove-Item -Recurse -Force C:\y }
          git clone --depth 1 "https://github.com/ysports-official/YSPORT.git" C:\y
          Write-Host "Klonlama tamamlandi: C:\y"
          Get-ChildItem C:\y | Select-Object Name

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: 🚀 Install EAS CLI
        run: npm install -g eas-cli

      - name: 📦 Install Project Dependencies
        shell: bash
        run: |
          cd /c/y/ysports-mobile
          npm install --legacy-peer-deps

      - name: 🔩 Android NDK Kontrol & Kurulum
        shell: powershell
        run: |
          $androidHome = "$env:LOCALAPPDATA\Android\Sdk"
          $ndkVer      = "27.1.12297006"
          $ndkPath     = "$androidHome\ndk\$ndkVer"
          if (Test-Path "$ndkPath\source.properties") {
            Write-Host "NDK OK: $ndkVer"
          } else {
            Write-Host "NDK eksik, kuruluyor..."
            $env:JAVA_HOME    = "$env:LOCALAPPDATA\Java\jdk-17.0.11+9"
            $env:ANDROID_HOME = $androidHome
            $env:PATH         = "$env:JAVA_HOME\bin;$androidHome\cmdline-tools\latest\bin;$env:PATH"
            cmd /c "`"$androidHome\cmdline-tools\latest\bin\sdkmanager.bat`" `"ndk;$ndkVer`""
            if ((Test-Path "$androidHome\ndk\$ndkVer-2") -and !(Test-Path "$ndkPath\source.properties")) {
              Rename-Item "$androidHome\ndk\$ndkVer-2" $ndkVer
            }
            Write-Host "NDK kuruldu: $(Test-Path `"$ndkPath\source.properties`")"
          }

      - name: 🏗️ Expo Prebuild (C:\y kisa path)
        shell: bash
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
        run: |
          cd /c/y/ysports-mobile
          npx expo prebuild --platform android --non-interactive --clean
          echo "Prebuild tamamlandi."
          ls android/app/

      - name: 🔨 Gradle Build (C:\y\ysports-mobile\android — kisa path)
        shell: bash
        env:
          JAVA_HOME:    ${{ env.JAVA_HOME }}
          ANDROID_HOME: ${{ env.ANDROID_HOME }}
        run: |
          PROFILE="${{ github.event.inputs.profile || 'preview' }}"
          echo "Profile: $PROFILE"

          KEYSTORE_WIN=$(powershell.exe -Command "echo \$env:USERPROFILE" | tr -d '\r')
          KEYSTORE="$KEYSTORE_WIN/.android/debug.keystore"

          cd /c/y/ysports-mobile/android

          if [ "$PROFILE" = "production" ]; then
            echo "AAB (bundleRelease) build baslatiliyor..."
            ./gradlew bundleRelease --no-daemon \
              -Pandroid.injected.signing.store.file="$KEYSTORE" \
              -Pandroid.injected.signing.store.password=android \
              -Pandroid.injected.signing.key.alias=androiddebugkey \
              -Pandroid.injected.signing.key.password=android
            cp app/build/outputs/bundle/release/app-release.aab "$GITHUB_WORKSPACE/app-release.aab"
            echo "BUILD_EXT=aab" >> $GITHUB_ENV
          else
            echo "APK (assembleDebug) build baslatiliyor — ProGuard kapali, standalone bundle..."
            ./gradlew assembleDebug --no-daemon \
              -PbundleInDebug=true
            cp app/build/outputs/apk/debug/app-debug.apk "$GITHUB_WORKSPACE/app-release.apk"
            echo "BUILD_EXT=apk" >> $GITHUB_ENV
          fi

          ls -lh "$GITHUB_WORKSPACE/app-release."*

      - name: 🚀 GitHub Release olarak yayinla
        shell: bash
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          PROFILE="${{ github.event.inputs.profile || 'preview' }}"
          TAG="build-${{ github.run_number }}"
          SHORT_SHA="${{ github.sha }}"
          SHORT_SHA="${SHORT_SHA:0:7}"

          cd "$GITHUB_WORKSPACE"
          gh release create "$TAG" app-release.* \
            --title "Build #${{ github.run_number }} - ${PROFILE} - ${SHORT_SHA}" \
            --notes "**Branch:** ${{ github.ref_name }}
          **Commit:** ${{ github.sha }}
          **Profile:** ${PROFILE}

          > APK indirmek icin asagidaki Assets bolumunu kullanin." \
            --prerelease \
            --repo ysports-official/YSPORT

      - name: Build Summary
        if: success()
        shell: bash
        run: |
          echo "## Build #${{ github.run_number }} Tamamlandi" >> $GITHUB_STEP_SUMMARY
          echo "Release: https://github.com/ysports-official/YSPORT/releases/tag/build-${{ github.run_number }}" >> $GITHUB_STEP_SUMMARY
          echo "APK'yi Releases sayfasindan indir" >> $GITHUB_STEP_SUMMARY
